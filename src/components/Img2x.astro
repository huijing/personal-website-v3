---
const { fileName, alt, fileType, class: className } = Astro.props;
---

<img
  src=`/images/${fileName}.${fileType}`
  srcset=`/images/${fileName}@2x.${fileType} 2x`
  alt={alt}
  class={className}
/>
